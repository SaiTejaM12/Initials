var inA, inB;


addEventListener(event:"click"{
var   
var displayNumber = displayNumber + click.textContent;
}

)

function add(inA, inB) {
  return (inA += inB);
}
function subtract(inA, inB) {
  return (inA -= inB);
}
function divide(inA, inB) {
  return (inA /= inB);
}
function multiply(inA, inB) {
  return (inA *= inB);
}


